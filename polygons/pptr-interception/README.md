# Puppeteer interception handle

Demonstrates request interception in puppeteer.

## Commands

 * `make && make run` - creates screenshot in artifacts dir
 * `make clean` - remove `node_modules` and `artifacts`
